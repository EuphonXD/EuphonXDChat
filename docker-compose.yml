version: '3.8'

services:
  webchat:
    build: .
    container_name: webchat
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - JWT_SECRET=change-me-in-production
      - PORT=3001
      - NODE_ENV=production
